class Vector {
    init(x, y) {
        this.x = x;
        this.y = y;
    }

    lengthSquared() {
        return this.x * this.x + this.y * this.y;
    }

    describe() {
        print "vector";
        print this.lengthSquared();
    }
}

var vector = Vector(3, 4);
vector.describe();
